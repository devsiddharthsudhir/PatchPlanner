# Changelog

## 0.2.0 — “Ops-ready polish”
- Add config via `.env` / env vars (CORS, API base URL)
- Add health/version endpoints
- Improve optimization explanations (“why scheduled” + “why deferred”)
- Add scenario presets (Risk-first / Cost-first / Outage-first) in UI
- Add docs: architecture + optimizer notes + demo script
- Add Makefile and GitHub Actions CI workflow

## 0.1.0 — Initial demo
- FastAPI + PuLP multi-objective patch scheduling
- React UI with sliders, schedule board, backlog, and patch detail drawer
